"""Networking agent — DEFERRED to Phase 3+ (see ARCHITECTURE.md 12).

No automated sending under any configuration. When built, research uses public,
non-LinkedIn sources first; drafts connection notes/follow-ups for human approval.
The DB entities (Recruiter, NetworkingContact, ConnectionRequest, Message) already exist
so no schema reshape is needed later.
"""
