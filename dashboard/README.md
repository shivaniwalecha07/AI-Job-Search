# Dashboard (Phase 2)

FastAPI + a lightweight frontend. Planned views (see ARCHITECTURE.md 17):
view/filter/sort jobs by match score, sponsorship evidence view, resume-match view,
shortlist, approve/reject applications, review answers, track status.

Because LinkedIn discovery is supervised (you run it interactively), a minimal
read-only dashboard may be worth pulling forward into Phase 1 — flagged as an open
question in ARCHITECTURE.md 18.
